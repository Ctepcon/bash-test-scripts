#!/bin/bash

# ul-param: демонстрация преобразования регистра символов
# с использованием подстановки параметров
if [[ "$1" ]]; then
    echo "${1,,}" # весь в нижний
    echo "${1,}"  # первый в нижний
    echo "${1^^}" # весь в верхний
    echo "${1^}"  # первый в верхний
fi